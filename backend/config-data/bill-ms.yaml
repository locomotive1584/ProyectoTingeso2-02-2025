server:
  port: 8001
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bill-db
    username: postgres
    password: 123
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka